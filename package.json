{
  "name": "multi-model-mcp-plugin",
  "version": "0.1.0",
  "description": "MCP Server plugin for Claude Code multi-model routing via subprocess",
  "type": "module",
  "bin": {
    "multi-model-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
